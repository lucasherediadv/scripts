#!/usr/bin/env bash

command -v docker >/dev/null 2>&1 || { echo "This script requires docker"; exit 1; }

all=$(docker ps -aq)

if [[ -z "$all" ]];then
  echo "No containers found."
  exit
fi

docker rm "$all"
